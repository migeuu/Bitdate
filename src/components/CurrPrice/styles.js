import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerPrice: {
    width: "100%",
    height: "auto",
    alignItems: "center",
  },
  currPrice: {
    color: "#daa520",
    fontSize: 32,
    fontWeight: "bold",
    paddingTop: 25,
  },
  textPrice: {
    color: "#ffffe6",
    marginBottom: 16,
    fontSize: 12,
    letterSpacing: 0.7,
    fontWeight: "bold",
  },
});
